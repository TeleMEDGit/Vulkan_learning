
#include "DrawVertex.h"
namespace Math
{



	/*int Math::ShadowVertex::CreateShadowCache(ShadowVertex * vertexCache, const DrawVertex * verts, const int numVerts)
	{
		for (int i = 0; i < numVerts; i++) {
			vertexCache[i * 2 + 0].xyzw.SetX(verts[i].xyz.GetX());
			vertexCache[i * 2 + 0].xyzw.SetY(verts[i].xyz.GetY());
			vertexCache[i * 2 + 0].xyzw.SetZ(verts[i].xyz.GetZ());
			vertexCache[i * 2 + 0].xyzw.SetW(1.0f);

			vertexCache[i * 2 + 1].xyzw.SetX(verts[i].xyz.GetX());
			vertexCache[i * 2 + 1].xyzw.SetX(verts[i].xyz.GetX());
			vertexCache[i * 2 + 1].xyzw.SetY(verts[i].xyz.GetX());
			vertexCache[i * 2 + 1].xyzw.SetW(0.0f);
		}
		return numVerts * 2;
	}*/
}


